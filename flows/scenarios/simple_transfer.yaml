# Category 1 (simple) - transfer lifecycle:
#   create -> assign -> investigation -> make decision (transfer) -> Transferred.
id: simple_transfer
name: Simple - reclamation transfer lifecycle
tags: [simple, lifecycle, write, manual]
steps:
  - use: auth.login
  - use: reclamation.create_reclamation
  - use: reclamation.assign
  - use: reclamation.take_to_work
  - use: reclamation.make_decision_transfer

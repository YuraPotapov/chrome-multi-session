"""Domain models for the flow-execution engine (flows, steps, results).

These are plain dataclasses with no browser or I/O dependencies, so they can be
imported and unit-tested without Playwright installed.
"""
